{
  "name": "ai-toolhouse",
  "version": "1.1.5",
  "description": "AI Toolhouse — generative AI pipeline for studio workstations",
  "author": "Studio",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:electron": "tsc -p electron/tsconfig.json && node -e \"const fs=require('fs'); fs.copyFileSync('electron/settings.html','dist-electron/settings.html'); fs.copyFileSync('electron/sync.html','dist-electron/sync.html'); fs.copyFileSync('electron/model-upload.html','dist-electron/model-upload.html')\"",
    "start": "npm run build:frontend && npm run build:electron && electron .",
    "build": "npm run build:frontend && npm run build:electron && electron-builder",
    "build:win": "npm run build:frontend && npm run build:electron && electron-builder --win"
  },
  "devDependencies": {
    "@types/electron": "^1.6.10",
    "@types/node": "^20.0.0",
    "electron": "^31.0.0",
    "electron-builder": "^24.13.3",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "electron-store": "^8.2.0",
    "electron-updater": "^6.8.3"
  }
}
